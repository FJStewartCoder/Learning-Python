# Task
Since sets and dictionaries may not be needed, feel free to skip this task.  

Create a list of dictionaries, each dictionary should a a profile on a person (name, age and favourite food).   
Loop through each dictionary and print out the name of each person and their age.  

# Bonus Task
Create a set with several names in it, loop through a second list of names and check if the name exists in the set.  
If it exists, output that the name exists.  