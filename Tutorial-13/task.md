# Task
Try to create some different error by trying different invalid actions.  
Examples:
- Creating a new file with 'x' mode that already exists
- String as int.

Then, use a try statement to handle the errors.

# Bonus Task
Create a user input system that handles the error when a user enters a word in an integer field.  
Raise some errors yourself.