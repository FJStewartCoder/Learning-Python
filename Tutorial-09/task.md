# Task
Get a user input from the user to get their name and age.  
If their age is bigger than 50, write message 1.  
Otherwise write message 2.

message 1: write their name in upper case, newline, and 'you are *age* years young'  
message 2: write their name in lower case, newline, and 'you are *age* years old'  

# Bonus Task
Complete the base task.  
Strip the name of whitespace, split it into each of their names.
Loop through each name and output it with first letter capitialised only.

Continue the rest of the message after. (Ignore the original method of writing the name).  
So, write name regardless then write the corresponding final message.

message 1: 'you are *age* years young'
message 1: 'you are *age* years old'